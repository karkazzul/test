version: '3.1'

services:

      
### torrent part ###

  test:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile

 
networks:
  zabbix:
    driver: bridge
